# --- Core (required) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # file uploads
requests>=2.31
beautifulsoup4>=4.12         # LinkedIn HTML parsing
pypdf>=4.0                   # PDF CV parsing
python-docx>=1.1             # DOCX CV parsing
scikit-learn>=1.4            # TF-IDF matching
jinja2>=3.1                  # template rendering

# --- Optional (better matching; larger download). Uncomment to enable. ---
# sentence-transformers>=2.6

# --- Optional (AI-written application drafts via Claude). Uncomment + set ANTHROPIC_API_KEY. ---
# anthropic>=0.40
